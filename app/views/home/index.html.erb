<div class="message container-fluid justify-content-center d-flex align-items-center"><%= t('welcome') %></div>

<div class="input-row container-fluid">
  <div class="row gx-2">
    <div class="col">
      <%= link_to t('buy_room'), booking_create_path, class: "btn btn-danger"%>
    </div>
    <div class="col">
      <%= link_to t('check-in'), booking_paid_path, class: "btn btn-success" %>
    </div>
  </div>
</div>
