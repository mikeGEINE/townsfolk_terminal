<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <div class="navbar-brand">Городские</div>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to "Терминал", root_path, class: "nav-link" %>
        </li>
        <% if current_user && current_user.admin? %>
          <li class="nav-item">
            <%= link_to "Номера", admin_view_rooms_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "Аккаунты", admin_view_accounts_path, class: "nav-link" %>
          </li>
        <% end %>
      </ul>
      <div class="d-flex align-items-center">
        <% if current_user %>
          <p class="my-0 mx-1"><%= current_user.email %></p>
          <%= button_to "Выйти", destroy_user_session_path, method: :delete, class: "btn btn-outline-danger" %>
        <% end %>
      </div>
    </div>
  </div>
</nav>
